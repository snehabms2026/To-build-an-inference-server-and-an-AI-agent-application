// create requirements.txt , .env, dockerfile,app.py
Run docker desktop in the background them go back to VS code terminal.
//docker build -t amd-inference-server .
//docker run -p 8000:8000 --env-file .env amd-inference-server
//http://127.0.0.1:8000/docs
test the api endpoints on swagger UI.
hence the api is dockerised.
